import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromARGB(255, 44, 123, 234); // Birincil renk
  static const Color secondary = Colors.white; // İkincil renk (ör: arka plan)
  static const Color accent = Color.fromARGB(255, 249, 111, 111); // Yardımcı renk (ör: kenarlıklar)
  static const Color textPrimary = Colors.black; // Ana metin rengi
  static const Color textSecondary = Color.fromARGB(255, 44, 123, 234); // Yardımcı metin rengi
}